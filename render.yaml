services:
  - type: web
    name: event-manager
    env: python
    buildCommand: "./build.sh"
    startCommand: "./start.sh"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: event-db
          property: connectionString

databases:
  - name: event-db
